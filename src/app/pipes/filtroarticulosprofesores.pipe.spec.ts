import { FiltroarticulosprofesoresPipe } from './filtroarticulosprofesores.pipe';

describe('FiltroarticulosprofesoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroarticulosprofesoresPipe();
    expect(pipe).toBeTruthy();
  });
});
