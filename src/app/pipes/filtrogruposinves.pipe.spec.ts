import { FiltrogruposinvesPipe } from './filtrogruposinves.pipe';

describe('FiltrogruposinvesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrogruposinvesPipe();
    expect(pipe).toBeTruthy();
  });
});
