import { FiltroprofesoresPipe } from './filtroprofesores.pipe';

describe('FiltroprofesoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroprofesoresPipe();
    expect(pipe).toBeTruthy();
  });
});
