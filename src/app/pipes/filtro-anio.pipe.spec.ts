import { FiltroAnioPipe } from './filtro-anio.pipe';

describe('FiltroAnioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAnioPipe();
    expect(pipe).toBeTruthy();
  });
});
