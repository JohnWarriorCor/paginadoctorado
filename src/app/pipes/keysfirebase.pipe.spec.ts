import { KeysfirebasePipe } from './keysfirebase.pipe';

describe('KeysfirebasePipe', () => {
  it('create an instance', () => {
    const pipe = new KeysfirebasePipe();
    expect(pipe).toBeTruthy();
  });
});
