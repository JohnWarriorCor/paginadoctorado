import { FiltrotesisPipe } from './filtrotesis.pipe';

describe('FiltrotesisPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrotesisPipe();
    expect(pipe).toBeTruthy();
  });
});
