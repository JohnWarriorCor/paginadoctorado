import { ListinvertPipe } from './listinvert.pipe';

describe('ListinvertPipe', () => {
  it('create an instance', () => {
    const pipe = new ListinvertPipe();
    expect(pipe).toBeTruthy();
  });
});
